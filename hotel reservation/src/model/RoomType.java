package model;
//An enum is a special "class" that represents
// a group of constants (unchangeable variables, like final variables).

// create an enum(specifically listed), use the enum keyword (instead of class or interface),
// separate the constants with a comma and itshould be in uppercase letters.
  public enum RoomType {
    SINGLE,
    DOUBLE
}
